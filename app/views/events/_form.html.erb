
<%= simple_form_for [@team, @event] do |f| %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.input :start_time, as: :time %>
  <%= f.input :end_time, as: :time %>
  <%= f.submit "Submit Event", class:"btn btn-secondary"%>
<% end %>
